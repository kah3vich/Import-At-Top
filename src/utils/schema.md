```bash
└── ImportAtTop() # - Главная функция запуска расширения.
    ├── configApp # - Переменная для получение конфигурационных данных для работы от пользователя или исходных из расширения.
    | #
    ├── arrTriggerWordImport # - Переменная с массивами слов для определения импорта в коде. ( Нужно для разделение кода )
    | #
    ├── arrTriggerWordOther # - Переменная с массивами слов для определения основного кода. ( Нужно для разделение кода )
    | #
    ├── getPartCode() # - Разделение кода, через параметр (type) можно получить определенную часть кода, которая была передана в параметр (code)
    |   └── copyArray() # Копирование массива, чтобы избежать мутации
    | #
    ├── removeDuplicates()
```

# Variables

```bash
└── Variables # - Переменные для
    ├── configApp # - Переменная для получение конфигурационных данных для работы от пользователя или исходных из расширения.
    | #
    ├── arrTriggerWordImport # - Переменная с массивами слов для определения импорта в коде. ( Нужно для разделение кода )
    | #
    ├── arrTriggerWordOther # - Переменная с массивами слов для определения основного кода. ( Нужно для разделение кода )
    | #
    ├── getPartCode() # - Разделение кода, через параметр (type) можно получить определенную часть кода, которая была передана в параметр (code)
    |   └── copyArray() # Копирование массива, чтобы избежать мутации
    | #
    ├── removeDuplicates()
```
